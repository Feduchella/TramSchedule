# КАК ОБНОВИТЬ РАСПИСАНИЕ ТРАМВАЕВ

Когда расписание трамваев меняется, вам нужно сделать всего **3 простых шага**.

---

## Шаг 1. Отредактируйте текстовый файл

Все расписания хранятся в папке `data/` в виде обычных текстовых файлов:

| Файл | Что в нём |
|---|---|
| `route1_weekday.txt` | Маршрут 1 — будни |
| `route1_weekend.txt` | Маршрут 1 — выходные |
| `route2.txt` | Маршрут 2 — будни и выходные |
| `route15.txt` | Маршрут 15 — будни и выходные |

Откройте нужный файл в **Блокноте** (или любом текстовом редакторе).

### Формат файла (простой!)

Файл состоит из блоков. Каждый блок — это одна остановка:

```
по остановке Ц.рынок 1 прямое направление
05:30 (3) 05:49 (4) 06:26 (6) 06:46 (7) 07:04 (1)
08:01 (4) 08:19 (5) 08:38 (6) 08:57 (7)
```

Где:
- `05:30` — время отправления (часы:минуты)
- `(3)` — номер выхода (наряда)
- Времена можно писать в любом порядке, через пробелы
- Можно переносить на несколько строк

### Что можно менять:
- ✏️ **Изменить время** — просто поменяйте цифры: `05:30` → `05:35`
- ➕ **Добавить рейс** — допишите время и выход: `05:35 (3)`
- ❌ **Удалить рейс** — просто сотрите его из строки
- 🔄 **Заменить всё расписание** — замените содержимое файла целиком

### Что НЕ нужно трогать:
- Первую строку (`Расписание движения трамваев`)
- Строку с номером маршрута (`Маршрут 001 День недели будни...`)
- Слова `по остановке` и `прямое/обратное направление`

---

## Шаг 2. Запустите скрипт обновления

Откройте командную строку (Пуск → cmd) и выполните:

```
cd C:\Users\OWNER\.gemini\antigravity\scratch\TramScheduleVK
python update_schedule.py
```

Скрипт:
1. Прочитает все файлы из папки `data/`
2. Переименует сокращённые названия остановок в полные
3. Объединит данные всех маршрутов
4. Создаст обновлённый файл `schedule.js`

Вы увидите что-то вроде:
```
Найдено файлов: 4
Остановок: 32
Отправлений: 5956
Файл schedule.js обновлён
[OK] ГОТОВО!
```

---

## Шаг 3. Загрузите файл на хостинг

После того как `schedule.js` обновился, загрузите его туда, где размещено ваше приложение (VK Hosting, GitHub Pages или другой хостинг). Заменить нужно **только один файл** — `schedule.js`.

Остальные файлы (`index.html`, `styles.css`, `app.js`) менять **не нужно**.

---

## Часто задаваемые вопросы

**В: Как добавить новый маршрут?**
О: Создайте новый файл в папке `data/` (например, `route3.txt`) в том же формате. Добавьте описание маршрута в файл `app.js` в раздел `ROUTE_INFO`.

**В: Что если я ошибусь в формате?**
О: Скрипт просто пропустит строки, которые не сможет разобрать. Проверьте, что все времена записаны как `ЧЧ:ММ (номер_выхода)`.

**В: Могу ли я переименовать остановку?**
О: Да! Добавьте новую запись в таблицу `STOP_RENAME` в файле `update_schedule.py`.
